# Executor report vN

Executor: `<single owner id>`
Proof version: `vN`

| Item | Status | Evidence | Result |
| --- | --- | --- | --- |
| Locked inputs and hashes | FAIL | `<manifest/input-check path>` | Record the actual result. |
| Runtime adaptation | FAIL | `<adapter path and validator log>` | Record beat/scene/acceptance coverage. |
| Pinned HyperFrames gates | FAIL | `<lint/validate/inspect/check logs>` | Record errors, warnings, and intentional exceptions. |
| Acceptance, cue, chapter and density snapshots | FAIL | `<snapshot paths>` | Record actual visible states. |
| Widest caption | FAIL | `<active caption snapshot>` | Record safe area and one-group visibility. |
| PIP crop | NOT_APPLICABLE | `<manifest or crop snapshot>` | Explain the actual disposition. |
| Audio boundaries and tails | FAIL | `<media analysis and logs>` | Record first/last region, black tail, and silent tail. |
| Transition midpoints and readable holds | FAIL | `<snapshot paths>` | Record every required state. |
| Animation map | FAIL | `<animation-map path>` | Explain every flag and dead zone. |
| Contact sheet and full internal preview | FAIL | `<contact sheet and mp4>` | Record Montage and full-watch readiness. |
| Anti-PPT / AI-tell gate | FAIL | `<visible evidence paths>` | Record each hard rejection result. |
| Formal release | NOT_APPLICABLE | `<production spec>` | Deferred until separate confirmation. |

Replace every `FAIL` only after the linked evidence exists. Revisions stay with the executor named above.
